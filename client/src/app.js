import Autocomplete from "./autocomplete";

export default function App() {
    return (
        <div id="container">
            <img src="clock.png" alt="clock" />
            <Autocomplete />
        </div>
    );
}
